// 主题枚举
export enum ThemeEnum {
    LIGHT = 'light',
    DARK = 'dark'
}

// 菜单枚举
export enum MenuEnum {
    CATALOGUE = 'catalogue',
    MENU = 'menu',
    BUTTON = 'button',
    LINK = 'link',
    IFRAME = 'iframe'
}

// 屏幕枚举
export enum ScreenEnum {
    SM = 640,
    MD = 768,
    LG = 1024,
    XL = 1280
}